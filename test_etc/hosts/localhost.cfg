define host{
    use                linux-ssh
	contact_groups     admins
    host_name          localhost
    address            localhost
    check_command      check_ping
    _SSH_USER          shinken
}

